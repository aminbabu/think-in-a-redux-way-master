import { createSlice } from "@reduxjs/toolkit";

// assignments slice initial state
export const initialState = {};

// assignments slice
export const assignmentsSlice = createSlice({
  name: "assignments",
  initialState,
  reducers: {},
});
