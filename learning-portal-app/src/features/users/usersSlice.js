import { createSlice } from "@reduxjs/toolkit";

// users slice initial state
export const initialState = {};

// users slice
export const usersSlice = createSlice({
  name: "users",
  initialState,
  reducers: {},
});
