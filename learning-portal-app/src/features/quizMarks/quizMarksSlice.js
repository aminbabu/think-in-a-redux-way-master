import { createSlice } from "@reduxjs/toolkit";

// quizMarks slice initial state
export const initialState = {};

// quizMarks slice
export const quizMarksSlice = createSlice({
  name: "quizMarks",
  initialState,
  reducers: {},
});
