import { createSlice } from "@reduxjs/toolkit";

// assignmentMarks slice initial state
export const initialState = {};

// assignmentMarks slice
export const assignmentMarksSlice = createSlice({
  name: "assignmentMarks",
  initialState,
  reducers: {},
});
